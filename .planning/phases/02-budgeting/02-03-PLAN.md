---
phase: 02-budgeting
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - lib/presentation/screens/budget/budget_list_screen.dart
  - lib/presentation/screens/budget/budget_form_screen.dart
  - lib/presentation/screens/budget/budget_detail_screen.dart
  - lib/presentation/widgets/budget/budget_card.dart
  - lib/presentation/widgets/budget/budget_progress_bar.dart
  - lib/presentation/widgets/budget/budget_overview_card.dart
  - lib/presentation/widgets/budget/budget_category_card.dart
  - lib/presentation/navigation/routes/app_router.dart
  - lib/presentation/navigation/routes/app_routes.dart
  - lib/presentation/providers/budget/budget_providers.dart
  - lib/presentation/screens/transaction_list/transaction_list_screen.dart
  - test/presentation/widgets/budget/budget_progress_bar_test.dart
autonomous: true
requirements: [BUD-01, BUD-02, BUD-03, BUD-04, BUD-05, BUD-06, BUD-07]

must_haves:
  truths:
    - "User can create, edit, and delete monthly budgets per expense category"
    - "Budget progress shows color-coded indicators (green/yellow/red) per BUD-03"
    - "In-app SnackBar alerts appear when budget thresholds are crossed per BUD-04"
    - "Home screen budget overview card shows total budget, spent, remaining, overspending count per BUD-05 and D-08"
    - "Budget vs actual comparison per category with expandable transaction list per BUD-06 and D-11/D-12"
    - "Anggaran tab added to navigation with month navigation per D-04/D-05"
  artifacts:
    - path: "lib/presentation/screens/budget/budget_list_screen.dart"
      provides: "Main Anggaran tab screen with month navigation and budget list"
      contains: "class BudgetListScreen"
    - path: "lib/presentation/screens/budget/budget_form_screen.dart"
      provides: "Budget create/edit form with category picker and amount input"
      contains: "class BudgetFormScreen"
    - path: "lib/presentation/screens/budget/budget_detail_screen.dart"
      provides: "Budget vs actual per category with expandable transactions per D-11/D-12"
      contains: "class BudgetDetailScreen"
    - path: "lib/presentation/widgets/budget/budget_overview_card.dart"
      provides: "Home screen budget summary card per D-08/D-09/D-10"
      contains: "class BudgetOverviewCard"
    - path: "lib/presentation/widgets/budget/budget_progress_bar.dart"
      provides: "Color-coded progress bar (green 0-75%, yellow 75-100%, red >100%)"
      contains: "class BudgetProgressBar"
    - path: "lib/presentation/navigation/routes/app_router.dart"
      provides: "Updated with Anggaran tab in activeTabs"
      contains: "label: 'Anggaran'"
    - path: "lib/presentation/navigation/routes/app_routes.dart"
      provides: "Budget route constants"
      contains: "budgets"
  key_links:
    - from: "lib/presentation/screens/budget/budget_list_screen.dart"
      to: "lib/presentation/providers/budget/budget_providers.dart"
      via: "Riverpod watch for budget list state"
      pattern: "ref\\.watch.*budget"
    - from: "lib/presentation/widgets/budget/budget_overview_card.dart"
      to: "lib/presentation/navigation/routes/app_router.dart"
      via: "Tap navigates to Anggaran tab per D-09"
      pattern: "go.*anggaran|GoRouter"
    - from: "lib/presentation/screens/transaction_list/transaction_list_screen.dart"
      to: "lib/presentation/controllers/budget/budget_alert_controller.dart"
      via: "Alert check triggered after transaction save per D-03"
      pattern: "budgetAlert|checkAlert"
---

<objective>
Create all budget UI screens, widgets, and navigation integration. Add the Anggaran tab to the bottom navigation. Wire the budget overview card to the home screen. Connect the alert system to transaction save flow.

Purpose: Deliver the complete user-facing budgeting experience — the Anggaran tab with month navigation (D-04/D-05), budget CRUD forms, progress indicators (D-03/BUD-03), home screen summary card (D-08/D-09/D-10), and drill-down budget vs actual view (D-11/D-12).

Output: All budget screens, widgets, navigation integration, alert wiring.
</objective>

<execution_context>
@/home/masrobot/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/masrobot/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-budgeting/02-CONTEXT.md
@.planning/phases/02-budgeting/02-01-SUMMARY.md
@.planning/phases/02-budgeting/02-02-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- From Plans 01+02 — contracts this plan consumes -->

From lib/presentation/navigation/routes/app_router.dart (current state):
```dart
const activeTabs = [
  NavigationTabConfig(
    label: 'Transaksi',
    icon: Icons.receipt_long,
    activeIcon: Icons.receipt_long,
    route: AppRoutes.transactions,
    showFab: true,
  ),
  NavigationTabConfig(
    label: 'Laporan',
    icon: Icons.bar_chart,
    activeIcon: Icons.bar_chart,
    route: AppRoutes.reports,
    showFab: false,
  ),
];
```

From lib/presentation/navigation/routes/app_routes.dart:
```dart
class AppRoutes {
  static const String transactions = '/transactions';
  static const String reports = '/reports';
  // ... other routes
}
```

Budget providers available from Plan 02:
- budgetReadRepositoryProvider, budgetWriteRepositoryProvider, budgetQueryRepositoryProvider
- createBudgetUseCaseProvider, updateBudgetUseCaseProvider, deleteBudgetUseCaseProvider
- getBudgetsWithSpentUseCaseProvider, checkBudgetAlertsUseCaseProvider
- budgetAlertProvider, budgetFormControllerProvider, budgetAlertControllerProvider

Design system components:
- AppGlassContainer.glassCard(child) — for budget cards per D-11
- AppEmptyState — for no-budgets state per D-05
- AppSpacing.lg (16px), AppRadius.md (12px) — consistent spacing
- CurrencyFormatter — Rupiah formatting for amounts
- AppDateFormatter — date formatting for month labels
- AppColors.getGlassSurface(), AppColors.getGlassCard() — theme-aware colors
- ErrorMessageMapper.getUserMessage(e) — user-friendly Indonesian error messages per AGENTS.md rule
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Anggaran tab to navigation and create budget screen skeleton</name>
  <files>
    lib/presentation/navigation/routes/app_router.dart,
    lib/presentation/navigation/routes/app_routes.dart,
    lib/presentation/screens/budget/budget_list_screen.dart,
    lib/presentation/screens/budget/budget_form_screen.dart,
    lib/presentation/screens/budget/budget_detail_screen.dart,
    lib/presentation/widgets/budget/budget_progress_bar.dart,
    lib/presentation/widgets/budget/budget_card.dart,
    test/presentation/widgets/budget/budget_progress_bar_test.dart
  </files>
  <read_first>
    lib/presentation/navigation/routes/app_router.dart
    lib/presentation/navigation/routes/app_routes.dart
    lib/presentation/screens/transaction_list/transaction_list_screen.dart
    lib/presentation/screens/category_management_screen.dart
    lib/presentation/utils/glassmorphism/app_glass_container.dart
    lib/presentation/utils/responsive/app_spacing.dart
    lib/presentation/utils/responsive/app_radius.dart
    lib/presentation/utils/currency_formatter.dart
    lib/presentation/utils/app_colors.dart
    lib/presentation/utils/error/error_message_mapper.dart
    lib/presentation/widgets/base/base.dart
    lib/presentation/providers/budget/budget_providers.dart
    lib/domain/entities/budget_with_spent_entity.dart
    .planning/phases/02-budgeting/02-CONTEXT.md
  </read_first>
  <action>
    **Navigation Update** (`app_router.dart`):
    Add Anggaran tab between Transaksi and Laporan per D-04/D-05 and Phase 1 NavigationTabConfig comments:
    ```dart
    const activeTabs = [
      NavigationTabConfig(
        label: 'Transaksi',
        icon: Icons.receipt_long,
        activeIcon: Icons.receipt_long,
        route: AppRoutes.transactions,
        showFab: true,
      ),
      NavigationTabConfig(
        label: 'Anggaran',
        icon: Icons.account_balance_wallet,
        activeIcon: Icons.account_balance_wallet,
        route: AppRoutes.budgets,
        showFab: true,
      ),
      NavigationTabConfig(
        label: 'Laporan',
        icon: Icons.bar_chart,
        activeIcon: Icons.bar_chart,
        route: AppRoutes.reports,
        showFab: false,
      ),
    ];
    ```
    Add StatefulShellBranch for budgets route in createGoRouter, matching existing branch pattern.

    **Route Constants** (`app_routes.dart`):
    Add: `static const String budgets = '/budgets';`
    Add: `static const String budgetForm = '/budgets/form';`
    Add: `static const String budgetDetail = '/budgets/detail';`

    **BudgetProgressBar Widget** (`budget_progress_bar.dart`):
    - Stateful widget accepting: `progressPercent` (double), `height` (double, default 8.0), `borderRadius` (double, default 4.0)
    - Color logic per BUD-03/D-03:
      - 0-75%: Colors.green.shade400
      - 75-100%: Colors.orange.shade400
      - >100%: Colors.red.shade400
    - Use AnimatedContainer for smooth color transitions
    - Clip with RoundedRectangleBorder for rounded ends

    **BudgetCard Widget** (`budget_card.dart`):
    - Displays single budget item: category icon/name, budget amount, spent amount, progress bar
    - Uses AppGlassContainer.glassCard for container per D-11
    - Shows CurrencyFormatter for all amounts
    - BudgetProgressBar below amounts
    - Tap handler for expanding to detail view

    **BudgetListScreen** (`budget_list_screen.dart`):
    - Main Anggaran tab content per D-05
    - AppBar with month/year label and arrow buttons for month navigation per D-04
    - Body: watch budgetsWithSpentProvider for current month
    - States:
      - Loading: CircularProgressIndicator
      - Empty: AppEmptyState with "Tambah Anggaran" prompt per D-05
      - Data: ListView of BudgetCard widgets
    - FAB: Navigate to BudgetFormScreen for creating new budget
    - Month navigation: swipe + arrow buttons per D-04

    **BudgetFormScreen** (`budget_form_screen.dart`):
    - Create/edit mode based on route params
    - Fields: Category dropdown (expense categories only), Amount text field
    - Month/year auto-set from current month (or passed from route)
    - Submit calls BudgetFormController.create/update
    - Validation: amount > 0, category selected
    - Error display via ErrorMessageMapper.getUserMessage(e) — NEVER raw exception per AGENTS.md rule
    - Success: pop back to budget list

    **BudgetDetailScreen** (`budget_detail_screen.dart`):
    - Shows all budgets for a month as glass cards per D-11
    - Each card: category icon/name, budget amount, spent, remaining, color-coded progress
    - Tapping a card expands inline to show transaction list per D-12
    - Ordered by % spent descending per D-13
    - Transaction list shows date, amount, note for each expense transaction in that category/month

    **Widget Test** (`budget_progress_bar_test.dart`):
    - Test green color at 50%
    - Test yellow color at 80%
    - Test red color at 120%
    - Test zero progress
  </action>
  <verify>
    <automated>flutter test test/presentation/widgets/budget/budget_progress_bar_test.dart</automated>
  </verify>
  <done>
    - Anggaran tab appears in bottom navigation between Transaksi and Laporan
    - BudgetListScreen with month navigation (swipe + arrows)
    - BudgetFormScreen for create/edit with expense-category-only dropdown
    - BudgetDetailScreen with inline expandable transaction list
    - BudgetProgressBar with green/yellow/red color coding
    - All screens use design system (AppGlassContainer, AppSpacing, etc.)
    - No technical errors shown to users (ErrorMessageMapper used)
    - `flutter analyze` clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Create home screen budget card and wire alert system</name>
  <files>
    lib/presentation/widgets/budget/budget_overview_card.dart,
    lib/presentation/widgets/budget/budget_category_card.dart,
    lib/presentation/screens/transaction_list/transaction_list_screen.dart,
    lib/presentation/providers/budget/budget_providers.dart
  </files>
  <read_first>
    lib/presentation/screens/transaction_list/transaction_list_screen.dart
    lib/presentation/controllers/budget/budget_alert_controller.dart
    lib/presentation/providers/budget/budget_alert_provider.dart
    lib/presentation/providers/budget/budget_providers.dart
    lib/presentation/utils/glassmorphism/app_glass_container.dart
    lib/presentation/utils/currency_formatter.dart
    lib/presentation/utils/app_colors.dart
    lib/domain/entities/budget_with_spent_entity.dart
    .planning/phases/02-budgeting/02-CONTEXT.md
  </read_first>
  <action>
    **BudgetOverviewCard** (`budget_overview_card.dart`):
    - Compact card showing per D-08: total budget, total spent, remaining amount, overspending count
    - Uses AppGlassContainer.glassCard
    - Text: "Anggaran" header, month label, amounts in CurrencyFormatter
    - Overspending count as badge: "2 kategori overbudget" (red text)
    - Tap navigates to Anggaran tab per D-09: `context.go(AppRoutes.budgets)`
    - Only shown when budgets exist for current month per D-10 — parent controls visibility

    **BudgetCategoryCard** (`budget_category_card.dart`):
    - Used in BudgetDetailScreen per D-11
    - Glass card with: category icon, name, budget amount, spent, remaining
    - BudgetProgressBar showing progress
    - Expandable section per D-12: when tapped, shows transaction list inline
    - Transaction list items show: date (AppDateFormatter), amount (CurrencyFormatter), note

    **Wire alert to transaction save** (`transaction_list_screen.dart`):
    - After successful transaction save (add/update), invoke BudgetAlertController.checkAlertsAfterTransaction
    - Parameters: the saved transaction's categoryId, year, month (extract from transaction date)
    - If alert type is not 'none', show SnackBar per D-01:
      - Warning (75%): "⚠ Anggaran {categoryName} sudah 75% terpakai"
      - Limit (100%): "🔴 Anggaran {categoryName} sudah mencapai batas"
      - Over (>100%): "🚨 Anggaran {categoryName} melebihi batas!"
    - SnackBar duration: 4 seconds, no action button (dismisses automatically per D-01)
    - Import BudgetAlertController provider and call through ref.read

    **Add budget overview card to home screen:**
    In `transaction_list_screen.dart` (or the appropriate home/transaction screen):
    - Watch budgetsWithSpentProvider for current month
    - If budgets exist (list.isNotEmpty per D-10): show BudgetOverviewCard above transaction list
    - Calculate totals: sum all budget amounts → totalBudget, sum all spentAmounts → totalSpent, totalBudget - totalSpent → remaining, count where progressPercent > 100 → overspendingCount
    - Pass these to BudgetOverviewCard

    **Providers update** (`budget_providers.dart`):
    - Add `budgetsSummaryProvider` that computes summary data for the overview card:
      ```dart
      @riverpod
      Future<BudgetSummary> budgetsSummary(Ref ref) async {
        final now = DateTime.now();
        final useCase = ref.read(getBudgetsWithSpentUseCaseProvider);
        final result = await useCase(MonthParams(year: now.year, month: now.month));
        // compute totalBudget, totalSpent, remaining, overspendingCount
      }
      ```
    - Create BudgetSummary as a simple class (or Freezed) holding: totalBudget, totalSpent, remainingAmount, overspendingCount, hasBudgets (bool)
  </action>
  <verify>
    <automated>flutter analyze lib/presentation/widgets/budget/ lib/presentation/screens/transaction_list/ lib/presentation/screens/budget/</automated>
  </verify>
  <done>
    - BudgetOverviewCard on home screen showing total budget/spent/remaining/overspending per D-08
    - Card tap navigates to Anggaran tab per D-09
    - Card hidden when no budgets exist for current month per D-10
    - SnackBar alerts fire after transaction save per D-03 and BUD-04
    - Alert messages in Indonesian per D-01
    - All widgets use design system (AppGlassContainer, CurrencyFormatter, etc.)
    - `flutter analyze` clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Form Input | Amount field accepts free text — must validate |
| SnackBar → User | Alert content must not leak internal class names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | BudgetFormScreen amount field | mitigate | Parse and validate amount > 0 before submission; use TextFormField with keyboardType: TextInputType.number |
| T-02-07 | Information Disclosure | SnackBar alert messages | mitigate | Use hardcoded Indonesian messages per D-01 — never interpolate raw exception text |
| T-02-08 | Elevation of Privilege | Budget category dropdown | mitigate | Only show expense categories — filter out income categories at provider level |
</threat_model>

<verification>
1. `flutter test test/presentation/widgets/budget/budget_progress_bar_test.dart` — widget tests pass
2. `flutter analyze` — no errors across all budget files
3. Code generation: `flutter pub run build_runner build --delete-conflicting-outputs`
4. `flutter test` — all existing + new tests pass
</verification>

<success_criteria>
- Anggaran tab in bottom navigation (3 tabs total)
- Budget CRUD screens functional with design system
- BudgetProgressBar shows green/yellow/red at correct thresholds
- Home screen budget overview card with summary data
- Budget vs actual detail with expandable transactions
- SnackBar alerts fire exactly once at each threshold
- All screens use Indonesian text and design system
- All tests pass, analyzer clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-budgeting/02-03-SUMMARY.md`
</output>
