---
phase: 02-budgeting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/domain/entities/budget_entity.dart
  - lib/domain/entities/budget_with_spent_entity.dart
  - lib/domain/entities/budget_alert_status_entity.dart
  - lib/domain/repositories/budget/budget_read_repository.dart
  - lib/domain/repositories/budget/budget_write_repository.dart
  - lib/domain/repositories/budget/budget_query_repository.dart
  - lib/domain/repositories/budget/budget_repositories.dart
  - lib/data/models/budget_model.dart
  - lib/data/repositories/budget/budget_read_repository_impl.dart
  - lib/data/repositories/budget/budget_write_repository_impl.dart
  - lib/data/repositories/budget/budget_query_repository_impl.dart
  - test/domain/entities/budget_entity_test.dart
  - test/data/repositories/budget/budget_repository_impl_test.dart
autonomous: true
requirements: [BUD-01, BUD-07]

must_haves:
  truths:
    - "BudgetEntity with id, categoryId, year, month, amount, createdAt, updatedAt can be created and validated"
    - "Budget CRUD operations work through segregated repository interfaces"
    - "One budget per category per month is enforced (UNIQUE constraint respected at repository layer)"
    - "Budget spent amount can be queried by joining transactions and budgets tables"
    - "Expense-only validation is enforced at repository layer (no income categories)"
  artifacts:
    - path: "lib/domain/entities/budget_entity.dart"
      provides: "BudgetEntity Freezed data class"
      contains: "abstract class BudgetEntity"
    - path: "lib/domain/entities/budget_with_spent_entity.dart"
      provides: "BudgetWithSpentEntity combining budget + spent amount + progress"
      contains: "abstract class BudgetWithSpentEntity"
    - path: "lib/domain/entities/budget_alert_status_entity.dart"
      provides: "BudgetAlertStatus tracking which alerts have been shown"
      contains: "abstract class BudgetAlertStatus"
    - path: "lib/domain/repositories/budget/budget_read_repository.dart"
      provides: "BudgetReadRepository interface"
      exports: ["getBudgetsForMonth", "getBudgetById"]
    - path: "lib/domain/repositories/budget/budget_write_repository.dart"
      provides: "BudgetWriteRepository interface"
      exports: ["createBudget", "updateBudget", "deleteBudget"]
    - path: "lib/domain/repositories/budget/budget_query_repository.dart"
      provides: "BudgetQueryRepository for spent calculation"
      exports: ["getBudgetsWithSpent", "getBudgetSpent"]
    - path: "lib/data/models/budget_model.dart"
      provides: "BudgetModel for DB serialization"
      contains: "class BudgetModel"
    - path: "lib/data/repositories/budget/budget_read_repository_impl.dart"
      provides: "Budget read operations via SQLite"
    - path: "lib/data/repositories/budget/budget_write_repository_impl.dart"
      provides: "Budget CRUD with expense-only validation"
    - path: "lib/data/repositories/budget/budget_query_repository_impl.dart"
      provides: "Budget spent calculation via JOIN query"
    - path: "test/domain/entities/budget_entity_test.dart"
      provides: "Entity validation tests"
    - path: "test/data/repositories/budget/budget_repository_impl_test.dart"
      provides: "Repository implementation tests with sqflite_common_ffi"
  key_links:
    - from: "lib/data/repositories/budget/budget_query_repository_impl.dart"
      to: "lib/data/datasources/local/schema_manager.dart"
      via: "BudgetFields constants for SQL column names"
      pattern: "BudgetFields\\."
    - from: "lib/data/repositories/budget/budget_write_repository_impl.dart"
      to: "lib/data/datasources/local/database_helper.dart"
      via: "tableBudgets constant for table name"
      pattern: "DatabaseHelper\\.tableBudgets"
---

<objective>
Create the complete budget data layer: Freezed entities (BudgetEntity, BudgetWithSpentEntity, BudgetAlertStatus), segregated repository interfaces (Read, Write, Query), SQLite-backed implementations, and comprehensive tests.

Purpose: Establish the data foundation for budget CRUD and spent-amount calculation that Plans 02 and 03 will build upon. All repository interfaces follow ISP-segregated pattern matching existing transaction/category repositories.

Output: Domain entities, repository interfaces, data models, repository implementations, and tests.
</objective>

<execution_context>
@/home/masrobot/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/masrobot/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-budgeting/02-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Existing patterns the executor MUST follow. Extracted from codebase. -->

From lib/domain/entities/category_entity.dart:
```dart
@freezed
abstract class CategoryEntity with _$CategoryEntity {
  const CategoryEntity._();
  const factory CategoryEntity({
    int? id,
    required String name,
    required CategoryType type,
    required String color,
    String? icon,
    @Default(0) int sortOrder,
    @Default(true) bool isActive,
    required DateTime createdAt,
    required DateTime updatedAt,
  }) = _CategoryEntity;
}
```

From lib/domain/repositories/transaction/transaction_analytics_repository.dart:
```dart
abstract class TransactionAnalyticsRepository {
  Future<Result<MonthlySummaryEntity>> getMonthlySummary(String yearMonth);
  Future<Result<List<CategoryBreakdownEntity>>> getCategoryBreakdown(
    String yearMonth, TransactionType type,
  );
}
```

From lib/data/datasources/local/schema_manager.dart (budgets table schema):
```dart
// CREATE TABLE budgets (
//   id INTEGER PRIMARY KEY AUTOINCREMENT,
//   category_id INTEGER NOT NULL,
//   year INTEGER NOT NULL,
//   month INTEGER NOT NULL CHECK(month BETWEEN 1 AND 12),
//   amount REAL NOT NULL CHECK(amount > 0),
//   created_at TEXT NOT NULL,
//   updated_at TEXT NOT NULL,
//   FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE CASCADE,
//   UNIQUE (category_id, year, month)
// )

class BudgetFields {
  static const String id = 'id';
  static const String categoryId = 'category_id';
  static const String year = 'year';
  static const String month = 'month';
  static const String amount = 'amount';
  static const String createdAt = 'created_at';
  static const String updatedAt = 'updated_at';
}
```

From lib/data/datasources/local/database_helper.dart:
```dart
static const String tableBudgets = 'budgets';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create budget domain entities and repository interfaces</name>
  <files>
    lib/domain/entities/budget_entity.dart,
    lib/domain/entities/budget_with_spent_entity.dart,
    lib/domain/entities/budget_alert_status_entity.dart,
    lib/domain/repositories/budget/budget_read_repository.dart,
    lib/domain/repositories/budget/budget_write_repository.dart,
    lib/domain/repositories/budget/budget_query_repository.dart,
    lib/domain/repositories/budget/budget_repositories.dart,
    test/domain/entities/budget_entity_test.dart
  </files>
  <read_first>
    lib/domain/entities/category_entity.dart
    lib/domain/entities/category_breakdown_entity.dart
    lib/domain/repositories/transaction/transaction_analytics_repository.dart
    lib/domain/repositories/transaction/transaction_read_repository.dart
    lib/domain/repositories/category/category_read_repository.dart
    lib/domain/core/result.dart
    lib/data/datasources/local/schema_manager.dart
    .planning/phases/02-budgeting/02-CONTEXT.md
  </read_first>
  <behavior>
    - BudgetEntity creation with valid fields produces correct entity
    - BudgetEntity requires categoryId, year, month, amount, createdAt, updatedAt
    - BudgetWithSpentEntity holds budget + spentAmount + progressPercent + remainingAmount
    - BudgetAlertStatus holds budgetId + warningShownAt + limitShownAt + overShownAt (nullable DateTimes)
    - BudgetReadRepository interface has: getBudgetsForMonth(int year, int month), getBudgetById(int id)
    - BudgetWriteRepository interface has: createBudget, updateBudget, deleteBudget — all return Result<BudgetEntity>
    - BudgetQueryRepository interface has: getBudgetsWithSpent(int year, int month) returning Result<List<BudgetWithSpentEntity>>, getBudgetSpent(int budgetId) returning Result<double>
    - budget_repositories.dart barrel file exports all three interfaces
  </behavior>
  <action>
    Create 3 Freezed entity files following the CategoryEntity pattern exactly:
    - Use `@freezed abstract class` (NOT just `@freezed class`) per AGENTS.md rule
    - Use `const EntityName._()` private constructor for methods
    - Include `part 'entity_name.freezed.dart'` directive
    - BudgetEntity: id (int?), categoryId (int), year (int), month (int), amount (double), createdAt (DateTime), updatedAt (DateTime)
    - BudgetWithSpentEntity: budget (BudgetEntity), spentAmount (double, @Default(0)), progressPercent (double, @Default(0)), remainingAmount (double, @Default(0)). Add a computed getter `progressColor` returning a string color indicator ('green', 'yellow', 'red') based on 0-75% / 75-100% / >100% thresholds per D-03 and BUD-03.
    - BudgetAlertStatus: budgetId (int), warningShownAt (DateTime?), limitShownAt (DateTime?), overShownAt (DateTime?) — per D-02 alert tracking

    Create 3 repository interfaces following TransactionAnalyticsRepository pattern:
    - BudgetReadRepository: `getBudgetsForMonth(int year, int month)` → `Future<Result<List<BudgetEntity>>>`, `getBudgetById(int id)` → `Future<Result<BudgetEntity>>`
    - BudgetWriteRepository: `createBudget({required int categoryId, required int year, required int month, required double amount})` → `Future<Result<BudgetEntity>>`, `updateBudget({required int id, required double amount})` → `Future<Result<BudgetEntity>>`, `deleteBudget(int id)` → `Future<Result<void>>`
    - BudgetQueryRepository: `getBudgetsWithSpent(int year, int month)` → `Future<Result<List<BudgetWithSpentEntity>>>` (joins transactions to calculate spent per budget), `getBudgetSpentForCategory({required int categoryId, required int year, required int month})` → `Future<Result<double>>`
    - BudgetRepositories barrel file: `export 'budget_read_repository.dart';` etc.

    Create entity test file following existing test patterns:
    - Test BudgetEntity creation with valid data
    - Test BudgetWithSpentEntity progressColor getter: green at 50%, yellow at 85%, red at 110%
    - Test BudgetAlertStatus default null alert dates
  </action>
  <verify>
    <automated>flutter test test/domain/entities/budget_entity_test.dart</automated>
  </verify>
  <done>
    - BudgetEntity, BudgetWithSpentEntity, BudgetAlertStatus exist as Freezed abstract classes
    - 3 repository interfaces with proper Result<T> return types
    - Barrel file exports all interfaces
    - Entity tests pass
    - `flutter analyze` clean on new files
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create budget data model and repository implementations</name>
  <files>
    lib/data/models/budget_model.dart,
    lib/data/repositories/budget/budget_read_repository_impl.dart,
    lib/data/repositories/budget/budget_write_repository_impl.dart,
    lib/data/repositories/budget/budget_query_repository_impl.dart,
    test/data/repositories/budget/budget_repository_impl_test.dart
  </files>
  <read_first>
    lib/data/repositories/transaction/transaction_read_repository_impl.dart
    lib/data/repositories/transaction/transaction_analytics_repository_impl.dart
    lib/data/repositories/category/category_read_repository_impl.dart
    lib/data/datasources/local/local_data_source.dart
    lib/data/datasources/local/database_helper.dart
    lib/data/datasources/local/schema_manager.dart
    lib/domain/core/result.dart
    lib/domain/entities/budget_entity.dart
    lib/domain/entities/budget_with_spent_entity.dart
    lib/domain/repositories/budget/budget_read_repository.dart
    lib/domain/repositories/budget/budget_write_repository.dart
    lib/domain/repositories/budget/budget_query_repository.dart
    test/data/datasources/local/schema_migration_test.dart
  </read_first>
  <behavior>
    - BudgetModel.fromMap converts SQLite row to model, toMap converts model to SQLite row
    - BudgetReadRepositoryImpl.getBudgetsForMonth returns budgets for given year+month
    - BudgetWriteRepositoryImpl.createBudget enforces expense-category-only validation (rejects income categories)
    - BudgetWriteRepositoryImpl.createBudget returns DatabaseFailure on UNIQUE constraint violation (one budget per category per month per BUD-07)
    - BudgetQueryRepositoryImpl.getBudgetsWithSpent returns budgets with spentAmount calculated from expense transactions in that month
    - BudgetQueryRepositoryImpl.getBudgetSpentForCategory returns sum of expense transactions for a category in a month
  </behavior>
  <action>
    Create BudgetModel in `lib/data/models/budget_model.dart`:
    - Follow existing model patterns (fromMap/toMap using BudgetFields constants)
    - fromMap: map each BudgetFields constant to the corresponding field
    - toMap: convert entity fields back to map with BudgetFields keys
    - Include `fromEntity` factory and `toEntity` method

    Create BudgetReadRepositoryImpl:
    - Inject `LocalDataSource` via constructor (same pattern as TransactionReadRepositoryImpl)
    - `getBudgetsForMonth`: `SELECT * FROM ${DatabaseHelper.tableBudgets} WHERE ${BudgetFields.year} = ? AND ${BudgetFields.month} = ? ORDER BY ${BudgetFields.categoryId}`
    - `getBudgetById`: `SELECT * FROM ${DatabaseHelper.tableBudgets} WHERE ${BudgetFields.id} = ?`
    - Map results using BudgetModel.fromMap, convert to entities

    Create BudgetWriteRepositoryImpl:
    - Inject `LocalDataSource` via constructor
    - `createBudget`: First verify the category exists AND is expense type (enforce BUD-01 expense-only constraint per Phase 1 decision — SQLite CHECK doesn't support subqueries, so validation is at repository layer). Then INSERT. Catch UNIQUE constraint violation → return `Result.failure(DatabaseFailure('Budget untuk kategori ini sudah ada bulan ini'))` for BUD-07 one-per-category-per-month.
    - `updateBudget`: UPDATE by id, update amount and updatedAt timestamp
    - `deleteBudget`: DELETE by id (CASCADE handles category deletion from schema FK)
    - All operations wrap in try/catch returning `Result.failure(DatabaseFailure(...))` on error

    Create BudgetQueryRepositoryImpl:
    - Inject `LocalDataSource` via constructor
    - `getBudgetsWithSpent`: Query budgets for month, then for each budget calculate spent by summing expense transactions for that category in that month:
      ```sql
      SELECT COALESCE(SUM(t.amount), 0.0)
      FROM transactions t
      JOIN categories c ON t.category_id = c.id
      WHERE c.type = 'expense'
        AND t.category_id = ?
        AND strftime('%Y', t.date) = ?
        AND strftime('%m', t.date) = ?
      ```
      Use the year and month parameters to build the date filter. Calculate progressPercent = (spentAmount / budget.amount * 100), remainingAmount = budget.amount - spentAmount. Return List<BudgetWithSpentEntity>.
    - `getBudgetSpentForCategory`: Same spent calculation for a single category/month combination.

    Create test file `test/data/repositories/budget/budget_repository_impl_test.dart`:
    - Use sqflite_common_ffi pattern from `test/data/datasources/local/schema_migration_test.dart`
    - Set up in-memory database with schema v3 in setUpAll
    - Test cases:
      1. Create budget for expense category → success
      2. Create budget for income category → failure (expense-only validation)
      3. Create duplicate budget (same category + year + month) → failure (UNIQUE constraint per BUD-07)
      4. Get budgets for month → returns created budgets
      5. Update budget amount → success with new amount
      6. Delete budget → success, no longer retrievable
      7. Get budgets with spent → calculates spent from existing transactions
      8. Get budgets with no transactions → spent is 0.0
  </action>
  <verify>
    <automated>flutter test test/data/repositories/budget/budget_repository_impl_test.dart</automated>
  </verify>
  <done>
    - BudgetModel with fromMap/toMap conversion
    - 3 repository implementations using LocalDataSource and BudgetFields
    - Expense-only validation on create (rejects income categories)
    - UNIQUE constraint handling returns user-friendly DatabaseFailure
    - Spent calculation correctly JOINs transactions + categories tables
    - All repository implementation tests pass
    - `flutter analyze` clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Repository | Untrusted input (categoryId, amount, year, month) crosses here |
| Repository → SQLite | SQL injection surface via parameter binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | BudgetWriteRepositoryImpl.createBudget | mitigate | Validate categoryId references existing expense category before INSERT |
| T-02-02 | Tampering | BudgetWriteRepositoryImpl.createBudget | mitigate | UNIQUE(category_id, year, month) constraint prevents duplicate budgets |
| T-02-03 | Information Disclosure | BudgetQueryRepositoryImpl | accept | All data is local, no network transmission; low-value target |
</threat_model>

<verification>
1. `flutter test test/domain/entities/budget_entity_test.dart` — entity tests pass
2. `flutter test test/data/repositories/budget/budget_repository_impl_test.dart` — repository tests pass
3. `flutter analyze` — no errors in new files
4. Verify Freezed code generation: `flutter pub run build_runner build --delete-conflicting-outputs`
</verification>

<success_criteria>
- BudgetEntity, BudgetWithSpentEntity, BudgetAlertStatus are valid Freezed classes
- 3 segregated repository interfaces exist with Result<T> return types
- Repository implementations handle CRUD + spent calculation via SQL JOIN
- Expense-only validation works (income category rejected)
- One-budget-per-category-per-month constraint enforced
- All tests pass, analyzer clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-budgeting/02-01-SUMMARY.md`
</output>
