---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/utils/app_colors.dart
  - lib/presentation/utils/app_theme.dart
  - lib/presentation/utils/glassmorphism/app_glass_container.dart
  - lib/presentation/utils/glassmorphism/app_glass_decoration.dart
  - lib/presentation/utils/glassmorphism/app_glass_blur.dart
  - lib/presentation/screens/transaction_list_screen.dart
  - lib/presentation/screens/transaction_form_screen.dart
  - lib/presentation/screens/monthly_summary_screen.dart
  - lib/presentation/screens/settings_screen.dart
  - lib/presentation/screens/category_management_screen.dart
  - lib/presentation/screens/scan_receipt_screen.dart
  - lib/presentation/widgets/transaction_card.dart
autonomous: true
requirements:
  - THM-01
  - THM-02
  - THM-03
  - THM-04
  - THM-05
  - THM-06

must_haves:
  truths:
    - "User can switch between Light, Dark, and System Default themes instantly without restart"
    - "Theme preference persists across app sessions via SharedPreferences"
    - "All existing UI components render correctly in dark mode with adjusted glassmorphism"
    - "App follows device theme in real-time when set to System Default"
    - "Dark theme follows Material Design dark guidelines (#121212 background family, elevated surfaces)"
    - "No hardcoded Colors.white, Colors.black, or Colors.grey remain in screen/widget files"
  artifacts:
    - path: "lib/presentation/utils/app_colors.dart"
      provides: "Dark mode glassmorphism alpha values per UI-SPEC redesign targets"
      contains: "getGlassCard"
    - path: "lib/presentation/utils/app_theme.dart"
      provides: "Complete dark ThemeData"
      contains: "darkTheme"
  key_links:
    - from: "lib/presentation/utils/app_colors.dart"
      to: "lib/presentation/utils/glassmorphism/"
      via: "Glassmorphism components use AppColors.getGlass* methods"
      pattern: "AppColors\\.getGlass"
    - from: "lib/presentation/screens/transaction_list_screen.dart"
      to: "lib/presentation/utils/app_colors.dart"
      via: "Screen uses theme-aware colors instead of hardcoded values"
      pattern: "AppColors\\."
---

<objective>
Dark mode audit and glassmorphism redesign — make every screen render correctly in both light and dark themes.

Purpose: Per THM-01 through THM-06, users must be able to switch themes instantly with all components rendering correctly. The existing dark theme infrastructure exists but glassmorphism needs redesign (D-09) and all hardcoded colors must be replaced (D-08).

Output: All screens and widgets use theme-aware colors. Glassmorphism dark mode redesigned per UI-SPEC targets.
</objective>

<execution_context>
@/home/masrobot/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/masrobot/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- Key patterns executor needs -->

From lib/presentation/utils/app_colors.dart:
```dart
class AppColors {
  // Existing dark mode helpers:
  static Color getGlassSurface({bool isDark = false, double? alpha})
  static Color getGlassCard({bool isDark = false, double? alpha})
  static Color getGlassPill({bool isDark = false, double? alpha})
  static Color getGlassNavigation({bool isDark = false, double? alpha})
  static Color getGlassOverlay({bool isDark = false, double? alpha})
  static Color getGlassBorder({bool isDark = false, double? alpha})
  static Color getGlassShadow({bool isDark = false, double? alpha})
  static Color getIncomeColor(bool isDark)
  static Color getExpenseColor(bool isDark)
  static Color getSurfaceColor(bool isDark)
  static Color getBackgroundColor(bool isDark)
}
```

From UI-SPEC.md Dark Mode Glassmorphism Redesign Targets:
| Component    | Min Alpha | Min Blur | Border alpha  | Shadow          |
|-------------|-----------|----------|---------------|-----------------|
| glassCard   | 0.80      | 12px     | 0.10-0.15     | Subtle depth    |
| glassSurface| 0.90      | 20px     | 0.10-0.15     | Medium depth    |
| glassPill   | 0.60      | 4px      | 0.08-0.12     | None            |
| glassNav    | 0.88      | 30px     | Top border    | Strong depth    |
| glassOverlay| 0.88      | 30px     | 0.12-0.15     | Strong depth    |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign glassmorphism dark mode alpha values and verify theme infrastructure</name>
  <files>
    - lib/presentation/utils/app_colors.dart
    - lib/presentation/utils/app_theme.dart
    - lib/presentation/utils/glassmorphism/app_glass_container.dart
    - lib/presentation/utils/glassmorphism/app_glass_decoration.dart
    - lib/presentation/utils/glassmorphism/app_glass_blur.dart
  </files>
  <read_first>
    - lib/presentation/utils/app_colors.dart
    - lib/presentation/utils/app_theme.dart
    - lib/presentation/utils/glassmorphism/app_glass_container.dart
    - lib/presentation/utils/glassmorphism/app_glass_decoration.dart
    - lib/presentation/utils/glassmorphism/app_glass_blur.dart
    - lib/presentation/providers/theme/theme_provider.dart
    - .planning/phases/01-foundation/01-UI-SPEC.md (Section: Dark Theme Elevation Strategy + Glassmorphism Redesign Targets)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08, D-09, D-10, D-11)
  </read_first>
  <acceptance_criteria>
    - `app_colors.dart` getGlassCard dark alpha is between 0.80 and 0.85
    - `app_colors.dart` getGlassSurface dark alpha is between 0.88 and 0.92
    - `app_colors.dart` getGlassNavigation dark alpha is 0.88
    - `app_colors.dart` getGlassOverlay dark alpha is 0.88
    - `app_colors.dart` getGlassBorder dark alpha is between 0.10 and 0.15
    - `app_theme.dart` darkTheme background color equals Color(0xFF221610)
    - `app_theme.dart` darkTheme uses elevated surface colors per Material Design dark guidelines
    - ThemeNotifier persistence works (already implemented — verify no regressions)
    - `grep -rn "Colors.white\|Colors.black" lib/presentation/utils/glassmorphism/` returns 0 matches (excluding Colors.transparent)
    - `flutter test` passes with all 954+ tests
  </acceptance_criteria>
  <action>
    Per D-09 (full glassmorphism redesign for dark mode) and UI-SPEC targets:

    1. **Update `app_colors.dart` dark mode alpha values per UI-SPEC redesign targets:**
       - `getGlassCard` dark alpha: 0.82 → 0.82 (within 0.80-0.85 range — current is acceptable per UI-SPEC)
       - `getGlassSurface` dark alpha: 0.92 → 0.90 (within 0.88-0.92 range)
       - `getGlassPill` dark alpha: 0.65 → 0.65 (within 0.60-0.70 range)
       - `getGlassNavigation` dark alpha: 0.88 → 0.88 (matches target)
       - `getGlassOverlay` dark alpha: 0.88 → 0.88 (matches target)
       - `getGlassBorder` dark alpha: 0.08 → 0.12 (increase for dark readability per UI-SPEC 0.10-0.15)
       - `getGlassShadow` dark alpha: 0.15 → 0.18 (slightly more for depth perception)

       Verify these values against the UI-SPEC table "Visual Requirements per Glass Component".

    2. **Update `app_theme.dart` dark theme:**
       - Verify `backgroundDark` (#221610) is used as scaffold background
       - Verify elevated surfaces use `surfaceDark` (#1A1210)
       - Verify all component theme overrides (ElevatedButton, InputDecoration, etc.) have dark mode counterparts
       - Per D-11: colors must follow Material Design dark theme guidelines

    3. **Update glassmorphism components to use theme-aware colors:**
       - `AppGlassContainer`: Verify it reads `isDark` from `Theme.of(context).brightness == Brightness.dark` and passes to AppColors methods
       - `AppGlassDecoration`: Same — verify isDark propagation
       - `AppGlassBlur`: Verify blur multipliers work in dark mode (UI-SPEC suggests 1.2-1.5× for dark)
       - Replace any `Colors.white` / `Colors.black` with `AppColors.getGlassBorder(isDark: isDark)` or equivalent

    4. **Do NOT change the ThemeNotifier or theme_provider.dart** — these already work correctly with SharedPreferences persistence and System/Light/Dark switching.
  </action>
  <verify>
    <automated>flutter test</automated>
  </verify>
  <done>
    - Glassmorphism alpha values updated to UI-SPEC targets
    - All glassmorphism components use theme-aware colors (no hardcoded Colors.white/black)
    - Dark ThemeData follows Material Design guidelines
    - All 954+ existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix hardcoded colors in all screens and widgets</name>
  <files>
    - lib/presentation/screens/transaction_list_screen.dart
    - lib/presentation/screens/transaction_form_screen.dart
    - lib/presentation/screens/monthly_summary_screen.dart
    - lib/presentation/screens/settings_screen.dart
    - lib/presentation/screens/category_management_screen.dart
    - lib/presentation/screens/scan_receipt_screen.dart
    - lib/presentation/widgets/transaction_card.dart
  </files>
  <read_first>
    - lib/presentation/utils/app_colors.dart (reference for replacement patterns)
    - lib/presentation/screens/transaction_list_screen.dart
    - lib/presentation/screens/transaction_form_screen.dart
    - lib/presentation/screens/monthly_summary_screen.dart
    - lib/presentation/screens/settings_screen.dart
    - lib/presentation/screens/category_management_screen.dart
    - lib/presentation/screens/scan_receipt_screen.dart
    - lib/presentation/widgets/transaction_card.dart
    - .planning/phases/01-foundation/01-UI-SPEC.md (Section: Grep Targets for Hardcoded Colors)
  </read_first>
  <acceptance_criteria>
    - `grep -rn "Colors\.white[^.]" lib/presentation/screens/ lib/presentation/widgets/` returns 0 matches
    - `grep -rn "Colors\.black[^.]" lib/presentation/screens/ lib/presentation/widgets/` returns 0 matches
    - `grep -rn "Colors\.grey[^.]" lib/presentation/screens/ lib/presentation/widgets/` returns 0 matches
    - `grep -rn "Color(0x" lib/presentation/screens/` returns 0 matches (no hardcoded hex in screens)
    - `flutter test` passes with all 954+ tests
    - `flutter analyze` passes with 0 errors
  </acceptance_criteria>
  <action>
    Per D-08 (full audit — check every screen and widget for hardcoded colors, fix all issues):

    **Search and replace pattern per UI-SPEC "Grep Targets for Hardcoded Colors":**

    1. Run grep to identify all violations:
       ```bash
       grep -rn "Colors\.white\|Colors\.black\|Colors\.grey\|Color(0x" lib/presentation/screens/ lib/presentation/widgets/
       ```

    2. For each violation, replace with theme-aware equivalent:
       - `Colors.white` → `Theme.of(context).colorScheme.surface` or `AppColors.getSurfaceColor(isDark)`
       - `Colors.black` → `Theme.of(context).colorScheme.onSurface` or `AppColors.textPrimary`
       - `Colors.grey` → `AppColors.textSecondary` or `AppColors.textTertiary` with isDark check
       - `Colors.grey.shadeXXX` → `Theme.of(context).colorScheme.outline` or `AppColors.textTertiary`
       - `Color(0xFF...)` → Use `AppColors` constants or theme `ColorScheme` properties
       - `Colors.transparent` → KEEP (no change needed)

    3. **Priority screens** (per UI-SPEC "Screens to Audit"):
       - HIGH: `transaction_list_screen.dart` — date group headers, amount colors, card backgrounds
       - HIGH: `transaction_form_screen.dart` — input fields, date picker, category selector backgrounds
       - HIGH: `monthly_summary_screen.dart` — chart colors, summary cards, glass containers
       - MEDIUM: `settings_screen.dart` — section headers, theme selector backgrounds
       - MEDIUM: `category_management_screen.dart` — reorderable list, icon/color picker backgrounds
       - MEDIUM: `scan_receipt_screen.dart` — camera overlay, scan results

    4. **Priority widgets** (per UI-SPEC "Widgets to Audit"):
       - HIGH: `TransactionCard` — hardcoded Colors.white backgrounds, amount colors
       - Verify: `AppEmptyState`, `AppErrorState`, `AppContainer` already use theme-aware colors

    5. For each replacement, determine `isDark` from context:
       ```dart
       final isDark = Theme.of(context).brightness == Brightness.dark;
       ```

    6. Do NOT change functional colors (income green, expense red) — these are already handled by `AppColors.getIncomeColor(isDark)` and `AppColors.getExpenseColor(isDark)`.

    7. Run full test suite after all changes to verify no regressions.
  </action>
  <verify>
    <automated>flutter test</automated>
  </verify>
  <done>
    - Zero hardcoded Colors.white, Colors.black, Colors.grey in screen and widget files
    - All screens render correctly in both light and dark themes
    - All 954+ existing tests pass
    - `flutter analyze` passes with 0 errors
  </done>
</task>

</tasks>

<verification>
1. `flutter test` — all tests pass (954+)
2. `grep -rn "Colors\.white[^.]" lib/presentation/screens/ lib/presentation/widgets/` — 0 matches
3. `grep -rn "Colors\.black[^.]" lib/presentation/screens/ lib/presentation/widgets/` — 0 matches
4. `flutter analyze` — 0 errors
5. Manual: Switch between Light/Dark/System themes — all screens render correctly
</verification>

<success_criteria>
- Theme switching works instantly (Light/Dark/System)
- Theme preference persists across sessions
- All glassmorphism components render correctly in dark mode with redesigned alpha values
- Zero hardcoded color literals in screen/widget files
- Dark theme follows Material Design guidelines per D-11
- All 954+ existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
