---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/datasources/local/schema_manager.dart
  - lib/data/datasources/local/database_helper.dart
autonomous: true
requirements:
  - THM-01
  - THM-02
  - THM-03
  - THM-04
  - THM-05
  - THM-06

must_haves:
  truths:
    - "DatabaseHelper exposes table name constants for budgets, savings_goals, and goal_contributions"
    - "DatabaseSchemaManager.currentVersion is 3"
    - "Fresh installs get all 3 new tables created in onCreate"
    - "Existing v2 databases are migrated to v3 via onUpgrade"
    - "Budgets table has unique constraint on (category_id, year, month)"
    - "Budgets table has CHECK constraint that category references expense-type only"
    - "Savings goals table has current_amount column and status TEXT column"
    - "Goal contributions table has amount (positive/negative) and running_balance columns"
  artifacts:
    - path: "lib/data/datasources/local/database_helper.dart"
      provides: "Table name constants for 3 new tables"
      contains: "tableBudgets"
    - path: "lib/data/datasources/local/schema_manager.dart"
      provides: "Schema v3 with 3 new tables + field constant classes"
      contains: "currentVersion = 3"
  key_links:
    - from: "lib/data/datasources/local/schema_manager.dart"
      to: "lib/data/datasources/local/database_helper.dart"
      via: "table name references in CREATE TABLE"
      pattern: "DatabaseHelper\\.table"
---

<objective>
Schema migration v2→v3 — add budgets, savings_goals, goal_contributions tables with proper constraints and indexes.

Purpose: Foundation data layer for Phase 2 (Budgeting) and Phase 3 (Savings Goals). Tables must exist before any feature code can be built.

Output: Updated `schema_manager.dart` (v3 with 3 new tables) and `database_helper.dart` (3 new table name constants).
</objective>

<execution_context>
@/home/masrobot/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/masrobot/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/data/datasources/local/database_helper.dart:
```dart
class DatabaseHelper {
  static const String tableTransactions = 'transactions';
  static const String tableCategories = 'categories';
  Future<Database> get database async { ... }
}
```

From lib/data/datasources/local/schema_manager.dart:
```dart
class DatabaseSchemaManager {
  static const int currentVersion = 2;
  static Future<void> onCreate(Database db, int version) async { ... }
  static Future<void> onUpgrade(Database db, int oldVersion, int newVersion) async { ... }
}

class TransactionFields {
  static const String id = 'id';
  static const String amount = 'amount';
  // ... field pattern
}

class CategoryFields {
  static const String id = 'id';
  static const String name = 'name';
  static const String type = 'type';  // 'income' | 'expense'
  // ... field pattern
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add table constants and field classes for 3 new tables</name>
  <files>lib/data/datasources/local/database_helper.dart, lib/data/datasources/local/schema_manager.dart</files>
  <read_first>
    - lib/data/datasources/local/database_helper.dart
    - lib/data/datasources/local/schema_manager.dart
    - .planning/phases/01-foundation/01-CONTEXT.md (D-12 through D-18 for table design decisions)
  </read_first>
  <behavior>
    - Test: DatabaseHelper.tableBudgets equals 'budgets'
    - Test: DatabaseHelper.tableSavingsGoals equals 'savings_goals'
    - Test: DatabaseHelper.tableGoalContributions equals 'goal_contributions'
    - Test: DatabaseSchemaManager.currentVersion equals 3
    - Test: BudgetFields has all required fields (id, categoryId, year, month, amount, createdAt, updatedAt)
    - Test: SavingsGoalFields has all required fields (id, name, targetAmount, currentAmount, status, etc.)
    - Test: GoalContributionFields has all required fields (id, goalId, amount, runningBalance, etc.)
  </behavior>
  <action>
    Per D-12 through D-18 in CONTEXT.md:

    1. In `database_helper.dart`, add 3 new table name constants:
       ```dart
       static const String tableBudgets = 'budgets';
       static const String tableSavingsGoals = 'savings_goals';
       static const String tableGoalContributions = 'goal_contributions';
       ```

    2. In `schema_manager.dart`:
       a. Change `currentVersion` from `2` to `3`
       b. Add field constant classes following the `TransactionFields`/`CategoryFields` pattern:

       **BudgetFields:**
       - `id` (INTEGER PRIMARY KEY AUTOINCREMENT)
       - `categoryId` (INTEGER NOT NULL) — references categories(id)
       - `year` (INTEGER NOT NULL)
       - `month` (INTEGER NOT NULL) — 1-12
       - `amount` (REAL NOT NULL CHECK > 0)
       - `createdAt` (TEXT NOT NULL)
       - `updatedAt` (TEXT NOT NULL)

       **SavingsGoalFields:**
       - `id` (INTEGER PRIMARY KEY AUTOINCREMENT)
       - `name` (TEXT NOT NULL)
       - `targetAmount` (REAL NOT NULL CHECK > 0)
       - `currentAmount` (REAL NOT NULL DEFAULT 0) — per D-13: stored, kept in sync
       - `targetDate` (TEXT) — nullable, optional deadline
       - `icon` (TEXT)
       - `color` (TEXT)
       - `status` (TEXT NOT NULL DEFAULT 'active' CHECK IN ('active', 'completed', 'cancelled')) — per D-15
       - `createdAt` (TEXT NOT NULL)
       - `updatedAt` (TEXT NOT NULL)

       **GoalContributionFields:**
       - `id` (INTEGER PRIMARY KEY AUTOINCREMENT)
       - `goalId` (INTEGER NOT NULL) — references savings_goals(id)
       - `amount` (REAL NOT NULL) — per D-14: positive=contribution, negative=withdrawal
       - `runningBalance` (REAL NOT NULL) — per D-16: stored, updated atomically
       - `note` (TEXT) — optional note/reason
       - `date` (TEXT NOT NULL)
       - `createdAt` (TEXT NOT NULL)

    3. Write tests in a new test file `test/data/datasources/local/schema_fields_test.dart` that verify:
       - All field constant values match expected column names
       - Table name constants are correct
       - currentVersion is 3
  </action>
  <verify>
    <automated>flutter test test/data/datasources/local/schema_fields_test.dart</automated>
  </verify>
  <done>
    - DatabaseHelper has 3 new table name constants
    - BudgetFields, SavingsGoalFields, GoalContributionFields classes exist with all required field constants
    - DatabaseSchemaManager.currentVersion is 3
    - All field constant tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CREATE TABLE statements and onUpgrade migration for v3</name>
  <files>lib/data/datasources/local/schema_manager.dart</files>
  <read_first>
    - lib/data/datasources/local/schema_manager.dart
    - lib/data/datasources/local/database_helper.dart
    - .planning/phases/01-foundation/01-CONTEXT.md (D-12 through D-18)
  </read_first>
  <behavior>
    - Test: Fresh DB creation (onCreate) creates budgets table with correct columns and constraints
    - Test: Fresh DB creation creates savings_goals table with status CHECK constraint
    - Test: Fresh DB creation creates goal_contributions table with foreign key to savings_goals
    - Test: Migration from v2 to v3 creates all 3 tables without data loss
    - Test: Budgets table has UNIQUE(category_id, year, month) constraint (per D-12)
    - Test: Budgets table has CHECK on category_id referencing expense type (per D-17)
    - Test: Savings goals has CHECK on status IN ('active','completed','cancelled') (per D-15)
  </behavior>
  <action>
    Per D-12 through D-18 in CONTEXT.md:

    1. Add private methods to `DatabaseSchemaManager`:
       - `_createBudgetsTable(Database db)` — CREATE TABLE with:
         - All BudgetFields columns
         - UNIQUE(category_id, year, month) — per D-12
         - FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE CASCADE
       - `_createBudgetsIndexes(Database db)` — index on (year, month) for monthly queries
       - `_createSavingsGoalsTable(Database db)` — CREATE TABLE with:
         - All SavingsGoalFields columns
         - CHECK(status IN ('active', 'completed', 'cancelled')) — per D-15
       - `_createSavingsGoalsIndexes(Database db)` — index on status for active queries
       - `_createGoalContributionsTable(Database db)` — CREATE TABLE with:
         - All GoalContributionFields columns
         - FOREIGN KEY (goal_id) REFERENCES savings_goals(id) ON DELETE CASCADE
       - `_createGoalContributionsIndexes(Database db)` — index on goal_id for per-goal queries

    2. Update `onCreate()`: add calls to the 6 new methods after existing transaction table creation.

    3. Update `onUpgrade()`: add `if (oldVersion < 3)` block that calls the 6 new methods. Per D-18: simple incremental, no existing data modified.

    4. For D-17 (expense-only constraint): Add CHECK constraint in budgets table:
       ```sql
       CHECK(category_id IN (SELECT id FROM categories WHERE type = 'expense'))
       ```
       Note: SQLite doesn't support subqueries in CHECK. Instead, enforce this at the application/repository layer. Document this with a comment in the table creation. The UI will validate, and the repository implementation will enforce it.

    5. Write integration-style tests in `test/data/datasources/local/schema_migration_test.dart`:
       - Test `onCreate` creates all 5 tables (transactions, categories + 3 new)
       - Test `onUpgrade` from v2 to v3 adds 3 new tables
       - Test budgets UNIQUE constraint works (insert duplicate category_id+year+month fails)
       - Test savings_goals status CHECK constraint works (invalid status fails)
       - Test goal_contributions foreign key constraint works (invalid goal_id fails)
       - Use in-memory database (`sqflite_ffi` or `databaseFactory` for testing)
  </action>
  <verify>
    <automated>flutter test test/data/datasources/local/schema_migration_test.dart</automated>
  </verify>
  <done>
    - onCreate creates 5 tables total (2 existing + 3 new)
    - onUpgrade from v2→v3 adds 3 new tables without data loss
    - Budgets UNIQUE(category_id, year, month) constraint enforced
    - Savings goals status CHECK constraint enforced
    - All indexes created for query performance
    - All migration tests pass
  </done>
</task>

</tasks>

<verification>
1. `flutter test test/data/datasources/local/` — all schema tests pass
2. `grep -c "currentVersion = 3" lib/data/datasources/local/schema_manager.dart` returns 1
3. `grep -c "tableBudgets\|tableSavingsGoals\|tableGoalContributions" lib/data/datasources/local/database_helper.dart` returns 3
4. `flutter analyze` passes with 0 errors
</verification>

<success_criteria>
- Schema version is 3
- 3 new tables created on fresh install and on migration
- Budgets table has unique constraint on (category_id, year, month)
- Savings goals has status CHECK constraint
- Goal contributions has foreign key to savings_goals
- All existing tests still pass (954+ tests)
- All new schema tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
